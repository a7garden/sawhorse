# Sawhorse Design

이 문서는 Sawhorse의 화면과 문구를 구현하는 기준이다. 색상·글꼴은 `app/src/index.css`에서 추출했다. 컴포넌트의 세부 규격은 기존 작업대·편집기 스타일을 따른다. 아래 원칙과 상태 설명은 새 화면에도 적용할 제품 디자인 지침이다.

## Voice

차분하고 구체적인 한국어를 기본으로 한다. 사용자가 지금 결정할 것과 결정 후 일어날 일을 짧게 설명한다. 실행 접수, 실제 실행, 에이전트 완료, 사용자 확인을 구분한다. 실패나 대기를 성공으로 표현하지 않는다. 기술 정보는 커밋·검증·복구처럼 사용자의 판단에 도움이 될 때 노출한다.

## Narrative and principles

작업의 의도부터 결과 확인까지 맥락을 보존하는 로컬 작업 공간이다. 문서와 결정은 오래 남고, 실행은 그 기록에 연결된다.

- 의도 인박스와 구체화된 작업을 별도 단계로 보여준다.
- 현재 단계, 다음 행동, 멈춘 이유를 같은 위치에서 읽게 한다.
- 승인에는 검토한 문서 버전을 연결한다. 원본과 이력은 보존한다.
- 목록은 밀도 있게, 읽고 결정하는 상세 화면은 여백 있게 구성한다.
- 의미는 색상만으로 전달하지 않는다. 텍스트·아이콘·상태를 함께 쓴다.
- 기존 편집기, 버튼, 대화상자, 오류 표시를 재사용한다.

## Personas

의도를 빠르게 기록하는 사용자, 여러 설계를 검토하고 일괄 구현하는 담당자, 구현 결과와 되돌리기 영향을 확인하는 검토자가 같은 기록을 사용한다. 전문 용어를 알지 못해도 다음 행동을 찾을 수 있어야 한다.

## Color tokens

| CSS token | Light | Dark | 용도 |
|---|---|---|---|
| `--background` | `#ffffff` | `#1a1d1b` | 기본 배경 |
| `--foreground` | `#252a28` | `#e7eae5` | 본문 |
| `--card` | `#ffffff` | `#202420` | 카드·상세 표면 |
| `--primary` | `#365e4e` | `#a0c5aa` | 주 행동·선택 |
| `--primary-foreground` | `#ffffff` | `#182b1e` | 주 행동 위 텍스트 |
| `--muted` | `#f1f2ef` | `#272c28` | 보조 표면 |
| `--muted-foreground` | `#737b76` | `#9da59d` | 보조 설명 |
| `--border` | `#e3e6e1` | `#353c35` | 경계 |
| `--destructive` | `#b64d40` | `#e08270` | 오류·폐기 |
| `--success` | `#41765a` | `#84b893` | 성공 |
| `--warning` | `#c58b37` | `#d8ac65` | 주의·대기 |
| `--sidebar` | `#f4f5f2` | `#171a17` | 탐색 영역 |

색상을 화면마다 복제하지 않고 CSS 변수를 사용한다. 밝은 테마와 어두운 테마를 함께 확인한다.

## Typography and shape

글꼴은 Pretendard, -apple-system, BlinkMacSystemFont, Segoe UI, Malgun Gothic, sans-serif 순이다. 기본 글자 크기는 13px, 자간은 -0.01em이다. Markdown 문서의 읽기 영역과 코드에는 각각 기존 문서·코드 스타일을 적용한다. 기본 radius 토큰은 0.5rem이다. 여백과 모서리는 인접한 기존 컴포넌트의 규격을 재사용한다.

## States and interaction

의도 → 구체화 → 설계 → 승인 대기 → 구현 대기 → 구현 → 완료·미확인 → 완료를 구분한다. 인터뷰는 해당 작업 상세 안에 질문, 선택지, 자유 답변, 답변 기록을 제공한다. 승인과 실행 시작은 별도 행동이며, 결과 확인은 한 번으로 완료된다.

작업 화면은 의도 인박스를 별도 공간으로 두고, 구체화부터 완료까지의 칸반을 기본으로 표시한다. 의도는 작업 수에 포함하지 않는다. 정상 완료는 완료 칸에, 취소·폐기는 보관함에 남긴다. 상단에는 검토할 설계와 확인할 결과를 바로 찾는 진입점을 둔다.

실행 중에는 중복 요청을 막는다. 저장하지 않은 편집이 있으면 이동 전에 보존 여부를 확인한다. 폐기 전에는 영향받는 의존 작업을 표시한다. 빈 화면에는 첫 행동, 오류에는 실패 이유와 재시도 경로를 제공한다. 긴 보드는 가로 스크롤을, 긴 문서는 세로 스크롤을 명시적으로 제공한다.

## Motion and accessibility

움직임은 실행 중 상태와 화면 전환을 설명하는 데 한정한다. 기존 상태 표시 애니메이션을 재사용하고 장식적인 반복 동작을 추가하지 않는다. 키보드 초점, 버튼 이름, 탭·대화상자 의미를 유지하고 상태 메시지는 보조 기술로도 전달한다. 작은 화면에서도 주 행동과 오류가 잘리지 않아야 한다.

## Applying this document

프로젝트에 할당된 DESIGN.md가 우선한다. 할당이 없으면 저장소 루트의 DESIGN.md를 에이전트 맥락으로 제공한다. 추출한 문서는 검토·등록 후 프로젝트에 적용한다. 라이브러리 원본 수정과 프로젝트 재적용은 별도 행동이다.

문서 구성은 [DESIGN.md 소개](https://wikidocs.net/366539)의 토큰과 디자인 의도 함께 기록하기를 참고했다.
